exception:
    file: basic
    line: 10
    message: unhandled error
services:
    caller:
        methods:
            call:
                input: "com.input"
properties:
    com.input:
        type: "string"
        label: "optional"
            