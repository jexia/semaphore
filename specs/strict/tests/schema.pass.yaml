objects:
    input:
        fields:
            message:
                type: "string"
    output:
        fields:
            message:
                type: "string"
services:
    caller:
        methods:
            Open:
                input:
                    fields:
                        message:
                            type: "string"
                            label: "optional"
                output:
                    fields:
                        message:
                            type: "string"
                            label: "optional"