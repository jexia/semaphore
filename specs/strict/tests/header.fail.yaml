exception:
    message: cannot use type int32 for header.Amount in flow echo
services:
    caller:
        methods:
            Open:
                input:
                    fields:
                        message:
                            type: "string"
                            label: "optional"
                output:
                    fields:
                        message:
                            type: "string"
                            label: "optional"