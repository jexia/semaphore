exception:
    message: cannot use type (int32) for 'header.Amount' in flow 'echo', expected (string)
objects:
    input:
        type: "message"
        label: "optional"
        nested:
            amount:
                type: "int32"
services:
    caller:
        methods:
            Open:
                input:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"
                output:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"