services:
    caller:
        methods:
            Open:
                input:
                    fields:
                        message:
                            type: "string"
                            label: "optional"