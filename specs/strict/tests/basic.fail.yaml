exception:
    message: cannot use (string) type (int32) in 'message'
objects:
    input:
        fields:
            message:
                type: "string"
services:
    caller:
        methods:
            Open:
                input:
                    fields:
                        message:
                            type: "int32"
                            label: "optional"
                output:
                    fields:
                        message:
                            type: "int32"
                            label: "optional"