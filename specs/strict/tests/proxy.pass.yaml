objects:
    input:
        type: "message"
        label: "optional"
        nested:
            message:
                type: "string"
                label: "optional"
services:
    caller:
        methods:
            Open:
                input:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"
                output:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"
            Upload:
                input:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"
                output:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"