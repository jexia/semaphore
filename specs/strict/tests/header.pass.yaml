objects:
    input:
        type: "message"
        label: "optional"
        nested:
services:
    caller:
        methods:
            Open:
                input:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"
                output:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"