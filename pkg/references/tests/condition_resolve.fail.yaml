exception:
  message: "failed to resolve flow 'echo': failed to resolve node 'condition': failed to resolve map parameter: failed to resolve params: failed to resolve property 'input.header:Unknown': undefined reference 'input.header:unknown' in '{{ input.header:Unknown }}.input.header:Unknown'"
properties:
  com.input:
    label: "optional"
    template:
      message:
        "message":
          name: "message"
          label: "optional"
          template:
            scalar:
              type: "string"
  com.output:
    label: "optional"
    template:
      message:
        "message":
          name: "message"
          label: "optional"
          template:
            scalar:
              type: "string"
services:
  caller:
    methods:
      Open:
        input: "com.input"
        output: "com.output"
      Upload:
        input: "com.input"
        output: "com.output"
