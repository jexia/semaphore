properties:
    com.input:
        type: "enum"
        label: "optional"
        enum:
            UNKNOWN:
                position: 1
                description: "unknown status"
            PENDING:
                position: 2
                description: "pending status"
            