exception:
    message: undefined object 'unexpected' in schema collection
properties:
    com.input:
        type: "message"
        label: "optional"
        nested:
            message:
                type: "string"
                label: "optional"
    com.output:
        type: "message"
        label: "optional"
        nested:
            message:
                type: "string"
                label: "optional"
services:
    caller:
        methods:
            Open:
                input: "com.input"
                output: "com.output"
            Upload:
                input: "com.input"
                output: "com.output"