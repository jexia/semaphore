exception:
  file: basic
  line: 10
  message: unhandled error
services:
  caller:
    methods:
      call:
        input: "com.input"
properties:
  com.input:
    label: "optional"
    template:
      scalar:
        type: "string"
        default: "hello world"
