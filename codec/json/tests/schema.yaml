objects:
    complete:
        type: "message"
        label: "optional"
        nested:
            message:
                type: "string"
                label: "optional"
            nested:
                type: "message"
                label: "optional"
                nested:
                    value:
                        type: "string"
                        label: "optional"
            repeating:
                type: "message"
                label: "repeated"
                nested:
                    value:
                        type: "string"
                        label: "optional"
services:
    mock:
        methods:
            complete:
                input:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"
                        nested:
                            type: "message"
                            label: "optional"
                            nested:
                                value:
                                    type: "string"
                                    label: "optional"
                        repeating:
                            type: "message"
                            label: "repeated"
                            nested:
                                value:
                                    type: "string"
                                    label: "optional"
                output:
                    type: "message"
                    label: "optional"
                    nested:
                        id:
                            type: "int64"
                            label: "optional"
            simple:
                input:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "string"
                            label: "optional"
                output:
                    type: "message"
                    label: "optional"
                    nested:
            repeated:
                input:
                    type: "message"
                    label: "optional"
                    nested:
                        repeating:
                            type: "message"
                            label: "repeated"
                            nested:
                                value:
                                    type: "string"
                                    label: "optional"
                output:
                    type: "message"
                    label: "optional"
                    nested:
            nested:
                input:
                    type: "message"
                    label: "optional"
                    nested:
                        nested:
                            type: "message"
                            label: "optional"
                            nested:
                                value:
                                    type: "string"
                                    label: "optional"
                output:
                    type: "message"
                    label: "optional"
                    nested: