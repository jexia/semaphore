objects:
    complete:
        fields:
            message:
                type: "string"
                label: "optional"
            nested:
                type: "message"
                label: "optional"
                message:
                    fields:
                        value:
                            type: "string"
                            label: "optional"
            repeating:
                type: "message"
                label: "repeating"
                message:
                    fields:
                        value:
                            type: "string"
                            label: "optional"
    simple:
        fields:
            message:
                type: "string"
                label: "optional"
    nested:
        fields:
            nested:
                type: "message"
                label: "optional"
                message:
                    fields:
                        value:
                            type: "string"
                            label: "optional"
    repeated:
        fields:
            repeating:
                type: "message"
                label: "repeating"
                message:
                    fields:
                        value:
                            type: "string"
                            label: "optional"
services:
    test:
        methods:
            complete:
                input:
                    fields:
                        message:
                            type: "string"
                            label: "optional"
                        nested:
                            type: "message"
                            label: "optional"
                            message:
                                fields:
                                    value:
                                        type: "string"
                                        label: "optional"
                        repeating:
                            type: "message"
                            label: "repeating"
                            message:
                                fields:
                                    value:
                                        type: "string"
                                        label: "optional"
                output:
                    fields:
                        id:
                            type: "int64"
                            label: "optional"
    mock:
        methods:
            simple:
                input:
                    fields:
                        message:
                            type: "string"
                            label: "optional"
                output:
                    fields:
            nested:
                input:
                    fields:
                        nested:
                            type: "message"
                            label: "optional"
                            message:
                                fields:
                                    value:
                                        type: "string"
                                        label: "optional"
                output:
                    fields:
            repeated:
                input:
                    fields:
                        repeating:
                            type: "message"
                            label: "repeating"
                            message:
                                fields:
                                    value:
                                        type: "string"
                                        label: "optional"
                output:
                    fields: