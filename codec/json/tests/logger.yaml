services:
    Logger:
        methods:
            Append:
                input:
                    fields:
                        message:
                            type: "string"
                            label: "optional"
                        nested:
                            type: "message"
                            label: "optional"
                            message:
                                fields:
                                    value:
                                        type: "string"
                                        label: "optional"
                        repeating:
                            type: "message"
                            label: "repeating"
                            message:
                                fields:
                                    value:
                                        type: "string"
                                        label: "optional"
                output:
                    fields:
                        id:
                            type: "int64"
                            label: "optional"