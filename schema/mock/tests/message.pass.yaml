objects:
    input:
        type: "message"
        label: "optional"
        nested:
            message:
                type: "string"
                label: "optional"
    output:
        type: "message"
        label: "optional"
        nested:
            message:
                type: "string"
                label: "optional"