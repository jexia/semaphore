exception:
    file: basic
    line: 10
    message: unhandled error
services:
    caller:
        methods:
            call:
                input:
                    fields:
                        message: "string"