exception:
    message: cannot use (string) type (int32) in 'message'
objects:
    input:
        type: "message"
        label: "optional"
        nested:
            message:
                type: "string"
                label: "optional"
services:
    caller:
        methods:
            Open:
                input:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "int32"
                            label: "optional"
                output:
                    type: "message"
                    label: "optional"
                    nested:
                        message:
                            type: "int32"
                            label: "optional"